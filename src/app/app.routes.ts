import { Routes } from '@angular/router';
import { Home } from './components/layout/home/home';

/* Usando rutas agrego el home en raiz */
export const routes: Routes = [{ path: '', component: Home }];
